apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker-sample-app.fullname" . }}
  labels:
    {{- include "docker-sample-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker-sample-app.selectorLabels" . | nindent 4 }}
