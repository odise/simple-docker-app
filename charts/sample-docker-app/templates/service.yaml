apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-docker-app.fullname" . }}
  labels:
    {{- include "sample-docker-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-docker-app.selectorLabels" . | nindent 4 }}
